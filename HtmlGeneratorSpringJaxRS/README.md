HTML spring rest service 

Run it as a java application. 

curl http://localhost:8080/generateHtmlRgbDisplayCode -d '{"rgbValue": "rgb(255, 165, 0)" , "text" : "someText" }' -H 'Content-Type: application/json'

